#include "main.h"
/**
 * positive_or_negative - checks for positve or nagative numbers
 *
 * @i: parameter function
 *
 * Return: always 0
 */

void positive_or_negative(int i)
{
	if (1 < 0)
		printf("%d is nagative\n", i);
	else
		printf("%d is positive\n", i);
}
